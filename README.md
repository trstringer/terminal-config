# Neovim config

```bash
# Restore Lazy plugins.
cp ./lazy-lock.json ~/.config/nvim/
nvim
# :Lazy restore

# Restore Mason LSPs.
cp ./mason-lock.json ~/.config/nvim/
nvim
# :MasonLockRestore

# Restore editorconfig
cp ./.editorconfig ~
```
